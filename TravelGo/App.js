import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import First_page from './assets/screens/Auth/First_page';

export default function App() {
  return (
    <First_page/>
  );
}

const styles = StyleSheet.create({
});
